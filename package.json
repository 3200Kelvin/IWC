{
  "name": "iwc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "deploy": "npm run build && git add . && git commit -a -m\"deploy\" && git push",
    "deploy:prod": "rm -rf ../backend/public/* && cp -r dist/* ../backend/public/ && npm --prefix ../backend run deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^3.6.0",
    "mini-css-extract-plugin": "^2.7.5",
    "path": "^0.12.7",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@barba/core": "^2.10.3"
  }
}
